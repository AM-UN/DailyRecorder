<p id="notice"><%= notice %></p>
<h2>毎日の積み重ね</h2>

<%= link_to '達成したこと', new_record_path %>

<%= month_calendar events: @records do |date, records| %>
  <%= date.day %>

  <% records.each do |record| %>
    <div>
      <%= link_to record.content, record %>
    </div>
  <% end %>
<% end %>